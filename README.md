It connects to the GitHub API with your token.
It has a rate limiter to avoid sending too many requests too quickly.
It defines custom error classes for common API issues (like not found, rate limit, or server errors).
It can fetch user details and list repositories for a given GitHub username.
